{
  "Lazy Spec": {
    "prefix": [
      "lazy"
    ],
    "description": "create single lazy spec to install plugins",
    "body": [
      "---@type LazySpec",
      "return {",
      "\t\"${1:plugin name}\",",
      "\tevent = \"VeryLazy\",",
      "\t-- for all available options, refer to:",
      "\topts = {${0}},",
      "\t},",
      "}"
    ]
  }
}
