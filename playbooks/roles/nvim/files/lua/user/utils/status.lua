return {
  component = require "user.utils.status.component",
  condition = require "user.utils.status.condition",
  env = require "user.utils.status.env",
  heirline = require "user.utils.status.heirline",
  hl = require "user.utils.status.hl",
  init = require "user.utils.status.init",
  provider = require "user.utils.status.provider",
  utils = require "user.utils.status.utils",
}
